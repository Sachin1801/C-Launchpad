#include <iostream>
using namespace std;

bool compare(string s1, string s2)
{
}

void sort_string(int *a, int n, int i)
{
    while (i <= n)
    {

        if (compare()))
    }
}

int main()
{
    int n;
    cin >> n;
    string arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }

    sort_string(arr, n, 0);

    for (int i = 0; i < n; i++)
    {
        cout << arr[i] << endl;
    }
    return 0;
}