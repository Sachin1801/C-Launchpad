#include <iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;
    int a[1000];
    int i = 0;
    int s = 16; // Sum of two numbersw in the array to find
                // cin the array
    for (int j = 0; j < n; j++)
    {
        cin >> a[j];
    }
    int j = sizeof(a) / sizeof(int) - 1;
    while
}