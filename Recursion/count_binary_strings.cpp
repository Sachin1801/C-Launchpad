#include <iostream>
#include <cstring>
using namespace std;

int count_binary(int n)
{
    if (n == 0)
    {
        return 0;
    }
    int cnt = 0;
    if ()
}

int main()
{
    cin.getline(s);
    int n;

    cin >> n;
    int s[n];
    //cout << count_binary(n) << endl;
    return 0;
}